name: 2 | Publish posts
on:
  pull_request:
    branches:
      - master
      - feat/*
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Publish post
        run: ls