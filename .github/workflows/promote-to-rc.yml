name: 2 | Promote to RC
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: dev
      url: https://${{ vars.HOST }}
    steps:
      - name: Promote to RC
        run: ls
